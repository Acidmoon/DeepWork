## 1. Repository Baseline Audit

- [x] 1.1 Confirm there are no conflicting active changes or existing baseline specs before standardizing the current MVP.
- [x] 1.2 Review the current `main`, `preload`, `shared`, and `renderer` modules to separate completed behavior, unfinished MVP work, and structural deficiencies.

## 2. Baseline Artifact Authoring

- [x] 2.1 Write the proposal that defines why the current codebase needs a formal OpenSpec baseline and which capabilities it introduces.
- [x] 2.2 Write the design that records current architecture, completed work, unfinished MVP scope, structural gaps, and documentation-only constraints.
- [x] 2.3 Write baseline capability specs for desktop workbench panels, workspace context management, agent session handoff, and settings/panel extensibility.

## 3. Validation and Follow-up Readiness

- [x] 3.1 Validate the new change with OpenSpec CLI and resolve any schema or formatting issues.
- [x] 3.2 Use this baseline as the prerequisite for future changes instead of editing current source behavior without an explicit spec delta.
- [x] 3.3 Open focused follow-up changes for unfinished MVP workflows such as richer artifact preview/search, reserved-panel rollout, core extraction, and automated regression coverage.
