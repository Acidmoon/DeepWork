## 1. Add .gitattributes

- [x] 1.1 Create `.gitattributes` at repo root with `* text=auto`

## 2. Verify

- [x] 2.1 Run `git add .gitattributes` and `git diff --cached` — the file should be staged with LF line endings
- [x] 2.2 Run `git status` to confirm no unexpected side effects
